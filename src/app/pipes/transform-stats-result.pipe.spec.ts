import { TransformStatsResultPipe } from './transform-stats-result.pipe';

describe('TransformStatsResultPipe', () => {
  it('create an instance', () => {
    const pipe = new TransformStatsResultPipe();
    expect(pipe).toBeTruthy();
  });
});
